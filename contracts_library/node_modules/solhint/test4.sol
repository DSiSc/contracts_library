pragma solidity ^0.4.18;


contract SolhinTest {

    function get(

    )
        external pure returns (uint ct)
    { // solhint error: func-order on this line
        var (x, , , y,) = myContract.get(); // space-after-comma error
        var (x, , , y,) = myContract.get(); // space-after-comma error
        return 1;
    }

    function get1(

    )
        external pure returns (uint ct)
    { // solhint error: func-order on this line
        {
            uint256 a = 10;
        }

        uint256 b = 10;

        {
            uint256 c = 6;
        }

        var (x, , , y,) = myContract.get(); // space-after-comma error
        var (x, , , y,) = myContract.get(); // space-after-comma error
        return 1;
    }

}